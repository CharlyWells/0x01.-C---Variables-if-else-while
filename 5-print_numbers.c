#include <stdio.h>

/**
 *a program that prints all single digit numbers of base 10 starting from 0, followed by a new line.
 *return 0
 */
int main()
{
  int num = 0;
  while(num < 10){
    num++;
    printf("%d",num);
  }
return 0;
}
